<%= form_with model: [@user, @post, @post.comment.build] do |form| %>
  <%= form.text_area :text, placeholder: 'Write a comment...', class:"form-control" %>
  <br>
  
  <%= form.submit class: 'btn btn-dark' %>
  <br>
<% end %>